package cn.monkey.orm.reactive.thread;

// public class DefaultReactiveTransactionExecutor implements ReactiveTransactionExecutor {

// private static final Logger logger = LoggerFactory.getLogger(DefaultMultiThreadTransactionExecutor.class);

// todo
    /*
    @Override
    public <T> Flux<T> batchExecute(ReactiveTransactionManager transactionManager,
                                    TransactionDefinition transactionDefinition,
                                    List<T> tasks, Function<List<T>, Flux<T>> taskCallBack,
                                    int bucketSize) {
        List<List<T>> partition = Lists.partition(tasks, bucketSize);
        TransactionContextManager.currentContext()
                .flatMapMany(transactionContext -> {
                })
        Flux.fromIterable(partition)
                .flatMap(taskCallBack)
                .
    }
     */

//}
